"""
Unir las fuentes numéricas en una tabla semanal y archivar el snapshot.
(Stub Fase 0 — implementación en Fase 2.)
"""

import pandas as pd


def unir() -> pd.DataFrame:
    """Combina fx, cafe y clima en una tabla semanal tidy."""
    return pd.DataFrame()
